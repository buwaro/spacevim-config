# Set prefix to Ctrl-Space
# unbind C-b
# set -g prefix C-Space
# bind Space send-prefix
# set-option -g prefix C-Space

unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Use vim keys
set -gw mode-keys vi

# Disable escape waiting time
set -s escape-time 0

